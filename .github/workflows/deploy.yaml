name: Deploy
"on":
  workflow_dispatch:
concurrency: ${{ github.event.repository.name }}
permissions:
  id-token: write
  contents: read

jobs:
  do-nothing:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello!"
